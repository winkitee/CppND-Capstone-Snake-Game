#ifndef ROUTE_MODEL_H
#define ROUTE_MODEL_H

#include <memory>
#include "game.h"
#include "a_star.h"

class RouteModel
{
public:
  RouteModel();
  // void MoveGameHere(std::unique_ptr<Game> game);
  // void MoveNext(Game &game);

private:
  // std::unique_ptr<Game> _game; 
};

#endif